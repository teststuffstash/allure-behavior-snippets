# Reviewer-bot approves Renovate `automerge` dep bumps so auto-merge can complete. Thin caller →
# the reusable org workflow in homelab. See homelab FU-014/FU-041. Added for stack-onboarding
# consistency (oracle stack claim, context-only repo).
name: renovate-approve
on:
  pull_request_target:
    types: [opened, reopened, synchronize, labeled]
permissions: {}
jobs:
  approve:
    uses: teststuffstash/homelab/.github/workflows/renovate-approve.reusable.yml@3339f2f23defd8813ab69b6e31e6eb6c0cb861e2 # master
    secrets: inherit
